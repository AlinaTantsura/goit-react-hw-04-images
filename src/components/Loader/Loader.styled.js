import styled from "styled-components";

const LoaderStyled = styled.div(() => {
    return {
        height: '90px',
        width: '80px',
        position: 'absolute',
        left: '50%',
        top: '50%',
    }
});

export default LoaderStyled;